// Generado por models/gen_poly_addsub_testvectors.py — 306 casos
// derivados de kyber_ref.barrett_reduce(), oráculo validado en Fase 3.
// NO EDITAR A MANO.
    check(0, 0, /*is_sub=*/0, 0);  // vadd
    check(0, 0, /*is_sub=*/1, 0);  // vsub
    check(0, 1, /*is_sub=*/0, 1);  // vadd
    check(0, 1, /*is_sub=*/1, 3328);  // vsub
    check(1, 0, /*is_sub=*/0, 1);  // vadd
    check(1, 0, /*is_sub=*/1, 1);  // vsub
    check(3328, 3328, /*is_sub=*/0, 3327);  // vadd
    check(3328, 3328, /*is_sub=*/1, 0);  // vsub
    check(3328, 1, /*is_sub=*/0, 0);  // vadd
    check(3328, 1, /*is_sub=*/1, 3327);  // vsub
    check(1, 3328, /*is_sub=*/0, 0);  // vadd
    check(1, 3328, /*is_sub=*/1, 2);  // vsub
    check(791, 1124, /*is_sub=*/0, 1915);  // vadd
    check(791, 1124, /*is_sub=*/1, 2996);  // vsub
    check(659, 525, /*is_sub=*/0, 1184);  // vadd
    check(659, 525, /*is_sub=*/1, 134);  // vsub
    check(2298, 2903, /*is_sub=*/0, 1872);  // vadd
    check(2298, 2903, /*is_sub=*/1, 2724);  // vsub
    check(437, 628, /*is_sub=*/0, 1065);  // vadd
    check(437, 628, /*is_sub=*/1, 3138);  // vsub
    check(1046, 1686, /*is_sub=*/0, 2732);  // vadd
    check(1046, 1686, /*is_sub=*/1, 2689);  // vsub
    check(786, 2245, /*is_sub=*/0, 3031);  // vadd
    check(786, 2245, /*is_sub=*/1, 1870);  // vsub
    check(2929, 3083, /*is_sub=*/0, 2683);  // vadd
    check(2929, 3083, /*is_sub=*/1, 3175);  // vsub
    check(1158, 2456, /*is_sub=*/0, 285);  // vadd
    check(1158, 2456, /*is_sub=*/1, 2031);  // vsub
    check(1852, 79, /*is_sub=*/0, 1931);  // vadd
    check(1852, 79, /*is_sub=*/1, 1773);  // vsub
    check(518, 2385, /*is_sub=*/0, 2903);  // vadd
    check(518, 2385, /*is_sub=*/1, 1462);  // vsub
    check(142, 1991, /*is_sub=*/0, 2133);  // vadd
    check(142, 1991, /*is_sub=*/1, 1480);  // vsub
    check(1811, 172, /*is_sub=*/0, 1983);  // vadd
    check(1811, 172, /*is_sub=*/1, 1639);  // vsub
    check(432, 2771, /*is_sub=*/0, 3203);  // vadd
    check(432, 2771, /*is_sub=*/1, 990);  // vsub
    check(511, 350, /*is_sub=*/0, 861);  // vadd
    check(511, 350, /*is_sub=*/1, 161);  // vsub
    check(1335, 2446, /*is_sub=*/0, 452);  // vadd
    check(1335, 2446, /*is_sub=*/1, 2218);  // vsub
    check(2440, 3281, /*is_sub=*/0, 2392);  // vadd
    check(2440, 3281, /*is_sub=*/1, 2488);  // vsub
    check(2986, 1696, /*is_sub=*/0, 1353);  // vadd
    check(2986, 1696, /*is_sub=*/1, 1290);  // vsub
    check(2444, 1053, /*is_sub=*/0, 168);  // vadd
    check(2444, 1053, /*is_sub=*/1, 1391);  // vsub
    check(1323, 604, /*is_sub=*/0, 1927);  // vadd
    check(1323, 604, /*is_sub=*/1, 719);  // vsub
    check(3296, 2929, /*is_sub=*/0, 2896);  // vadd
    check(3296, 2929, /*is_sub=*/1, 367);  // vsub
    check(2598, 498, /*is_sub=*/0, 3096);  // vadd
    check(2598, 498, /*is_sub=*/1, 2100);  // vsub
    check(3096, 2200, /*is_sub=*/0, 1967);  // vadd
    check(3096, 2200, /*is_sub=*/1, 896);  // vsub
    check(2825, 2638, /*is_sub=*/0, 2134);  // vadd
    check(2825, 2638, /*is_sub=*/1, 187);  // vsub
    check(253, 2279, /*is_sub=*/0, 2532);  // vadd
    check(253, 2279, /*is_sub=*/1, 1303);  // vsub
    check(2675, 916, /*is_sub=*/0, 262);  // vadd
    check(2675, 916, /*is_sub=*/1, 1759);  // vsub
    check(1132, 1179, /*is_sub=*/0, 2311);  // vadd
    check(1132, 1179, /*is_sub=*/1, 3282);  // vsub
    check(1305, 641, /*is_sub=*/0, 1946);  // vadd
    check(1305, 641, /*is_sub=*/1, 664);  // vsub
    check(2967, 584, /*is_sub=*/0, 222);  // vadd
    check(2967, 584, /*is_sub=*/1, 2383);  // vsub
    check(3245, 2903, /*is_sub=*/0, 2819);  // vadd
    check(3245, 2903, /*is_sub=*/1, 342);  // vsub
    check(3275, 1247, /*is_sub=*/0, 1193);  // vadd
    check(3275, 1247, /*is_sub=*/1, 2028);  // vsub
    check(3110, 284, /*is_sub=*/0, 65);  // vadd
    check(3110, 284, /*is_sub=*/1, 2826);  // vsub
    check(1018, 312, /*is_sub=*/0, 1330);  // vadd
    check(1018, 312, /*is_sub=*/1, 706);  // vsub
    check(2375, 799, /*is_sub=*/0, 3174);  // vadd
    check(2375, 799, /*is_sub=*/1, 1576);  // vsub
    check(1193, 2807, /*is_sub=*/0, 671);  // vadd
    check(1193, 2807, /*is_sub=*/1, 1715);  // vsub
    check(2806, 3167, /*is_sub=*/0, 2644);  // vadd
    check(2806, 3167, /*is_sub=*/1, 2968);  // vsub
    check(2931, 1244, /*is_sub=*/0, 846);  // vadd
    check(2931, 1244, /*is_sub=*/1, 1687);  // vsub
    check(1103, 1682, /*is_sub=*/0, 2785);  // vadd
    check(1103, 1682, /*is_sub=*/1, 2750);  // vsub
    check(904, 468, /*is_sub=*/0, 1372);  // vadd
    check(904, 468, /*is_sub=*/1, 436);  // vsub
    check(1127, 1115, /*is_sub=*/0, 2242);  // vadd
    check(1127, 1115, /*is_sub=*/1, 12);  // vsub
    check(1961, 2178, /*is_sub=*/0, 810);  // vadd
    check(1961, 2178, /*is_sub=*/1, 3112);  // vsub
    check(851, 861, /*is_sub=*/0, 1712);  // vadd
    check(851, 861, /*is_sub=*/1, 3319);  // vsub
    check(292, 2738, /*is_sub=*/0, 3030);  // vadd
    check(292, 2738, /*is_sub=*/1, 883);  // vsub
    check(1326, 2788, /*is_sub=*/0, 785);  // vadd
    check(1326, 2788, /*is_sub=*/1, 1867);  // vsub
    check(2459, 3153, /*is_sub=*/0, 2283);  // vadd
    check(2459, 3153, /*is_sub=*/1, 2635);  // vsub
    check(2121, 1140, /*is_sub=*/0, 3261);  // vadd
    check(2121, 1140, /*is_sub=*/1, 981);  // vsub
    check(1745, 1673, /*is_sub=*/0, 89);  // vadd
    check(1745, 1673, /*is_sub=*/1, 72);  // vsub
    check(17, 2483, /*is_sub=*/0, 2500);  // vadd
    check(17, 2483, /*is_sub=*/1, 863);  // vsub
    check(2969, 2041, /*is_sub=*/0, 1681);  // vadd
    check(2969, 2041, /*is_sub=*/1, 928);  // vsub
    check(2825, 2035, /*is_sub=*/0, 1531);  // vadd
    check(2825, 2035, /*is_sub=*/1, 790);  // vsub
    check(206, 2625, /*is_sub=*/0, 2831);  // vadd
    check(206, 2625, /*is_sub=*/1, 910);  // vsub
    check(2680, 2178, /*is_sub=*/0, 1529);  // vadd
    check(2680, 2178, /*is_sub=*/1, 502);  // vsub
    check(2420, 785, /*is_sub=*/0, 3205);  // vadd
    check(2420, 785, /*is_sub=*/1, 1635);  // vsub
    check(1142, 1139, /*is_sub=*/0, 2281);  // vadd
    check(1142, 1139, /*is_sub=*/1, 3);  // vsub
    check(3116, 2871, /*is_sub=*/0, 2658);  // vadd
    check(3116, 2871, /*is_sub=*/1, 245);  // vsub
    check(1462, 2761, /*is_sub=*/0, 894);  // vadd
    check(1462, 2761, /*is_sub=*/1, 2030);  // vsub
    check(231, 1573, /*is_sub=*/0, 1804);  // vadd
    check(231, 1573, /*is_sub=*/1, 1987);  // vsub
    check(1768, 760, /*is_sub=*/0, 2528);  // vadd
    check(1768, 760, /*is_sub=*/1, 1008);  // vsub
    check(3009, 3006, /*is_sub=*/0, 2686);  // vadd
    check(3009, 3006, /*is_sub=*/1, 3);  // vsub
    check(895, 1236, /*is_sub=*/0, 2131);  // vadd
    check(895, 1236, /*is_sub=*/1, 2988);  // vsub
    check(827, 439, /*is_sub=*/0, 1266);  // vadd
    check(827, 439, /*is_sub=*/1, 388);  // vsub
    check(2715, 1577, /*is_sub=*/0, 963);  // vadd
    check(2715, 1577, /*is_sub=*/1, 1138);  // vsub
    check(2484, 984, /*is_sub=*/0, 139);  // vadd
    check(2484, 984, /*is_sub=*/1, 1500);  // vsub
    check(1363, 329, /*is_sub=*/0, 1692);  // vadd
    check(1363, 329, /*is_sub=*/1, 1034);  // vsub
    check(2316, 2471, /*is_sub=*/0, 1458);  // vadd
    check(2316, 2471, /*is_sub=*/1, 3174);  // vsub
    check(2040, 523, /*is_sub=*/0, 2563);  // vadd
    check(2040, 523, /*is_sub=*/1, 1517);  // vsub
    check(1072, 2892, /*is_sub=*/0, 635);  // vadd
    check(1072, 2892, /*is_sub=*/1, 1509);  // vsub
    check(671, 2007, /*is_sub=*/0, 2678);  // vadd
    check(671, 2007, /*is_sub=*/1, 1993);  // vsub
    check(2978, 1, /*is_sub=*/0, 2979);  // vadd
    check(2978, 1, /*is_sub=*/1, 2977);  // vsub
    check(56, 479, /*is_sub=*/0, 535);  // vadd
    check(56, 479, /*is_sub=*/1, 2906);  // vsub
    check(2946, 2745, /*is_sub=*/0, 2362);  // vadd
    check(2946, 2745, /*is_sub=*/1, 201);  // vsub
    check(533, 1507, /*is_sub=*/0, 2040);  // vadd
    check(533, 1507, /*is_sub=*/1, 2355);  // vsub
    check(784, 669, /*is_sub=*/0, 1453);  // vadd
    check(784, 669, /*is_sub=*/1, 115);  // vsub
    check(2966, 1828, /*is_sub=*/0, 1465);  // vadd
    check(2966, 1828, /*is_sub=*/1, 1138);  // vsub
    check(3321, 26, /*is_sub=*/0, 18);  // vadd
    check(3321, 26, /*is_sub=*/1, 3295);  // vsub
    check(610, 361, /*is_sub=*/0, 971);  // vadd
    check(610, 361, /*is_sub=*/1, 249);  // vsub
    check(2719, 2735, /*is_sub=*/0, 2125);  // vadd
    check(2719, 2735, /*is_sub=*/1, 3313);  // vsub
    check(1081, 2784, /*is_sub=*/0, 536);  // vadd
    check(1081, 2784, /*is_sub=*/1, 1626);  // vsub
    check(3212, 726, /*is_sub=*/0, 609);  // vadd
    check(3212, 726, /*is_sub=*/1, 2486);  // vsub
    check(1939, 536, /*is_sub=*/0, 2475);  // vadd
    check(1939, 536, /*is_sub=*/1, 1403);  // vsub
    check(1723, 526, /*is_sub=*/0, 2249);  // vadd
    check(1723, 526, /*is_sub=*/1, 1197);  // vsub
    check(1719, 2032, /*is_sub=*/0, 422);  // vadd
    check(1719, 2032, /*is_sub=*/1, 3016);  // vsub
    check(2783, 3019, /*is_sub=*/0, 2473);  // vadd
    check(2783, 3019, /*is_sub=*/1, 3093);  // vsub
    check(768, 2900, /*is_sub=*/0, 339);  // vadd
    check(768, 2900, /*is_sub=*/1, 1197);  // vsub
    check(297, 1518, /*is_sub=*/0, 1815);  // vadd
    check(297, 1518, /*is_sub=*/1, 2108);  // vsub
    check(298, 2480, /*is_sub=*/0, 2778);  // vadd
    check(298, 2480, /*is_sub=*/1, 1147);  // vsub
    check(2145, 338, /*is_sub=*/0, 2483);  // vadd
    check(2145, 338, /*is_sub=*/1, 1807);  // vsub
    check(357, 1876, /*is_sub=*/0, 2233);  // vadd
    check(357, 1876, /*is_sub=*/1, 1810);  // vsub
    check(2700, 1149, /*is_sub=*/0, 520);  // vadd
    check(2700, 1149, /*is_sub=*/1, 1551);  // vsub
    check(2439, 2613, /*is_sub=*/0, 1723);  // vadd
    check(2439, 2613, /*is_sub=*/1, 3155);  // vsub
    check(598, 131, /*is_sub=*/0, 729);  // vadd
    check(598, 131, /*is_sub=*/1, 467);  // vsub
    check(1755, 2921, /*is_sub=*/0, 1347);  // vadd
    check(1755, 2921, /*is_sub=*/1, 2163);  // vsub
    check(2296, 833, /*is_sub=*/0, 3129);  // vadd
    check(2296, 833, /*is_sub=*/1, 1463);  // vsub
    check(2973, 3175, /*is_sub=*/0, 2819);  // vadd
    check(2973, 3175, /*is_sub=*/1, 3127);  // vsub
    check(2928, 1928, /*is_sub=*/0, 1527);  // vadd
    check(2928, 1928, /*is_sub=*/1, 1000);  // vsub
    check(2435, 3315, /*is_sub=*/0, 2421);  // vadd
    check(2435, 3315, /*is_sub=*/1, 2449);  // vsub
    check(3102, 2937, /*is_sub=*/0, 2710);  // vadd
    check(3102, 2937, /*is_sub=*/1, 165);  // vsub
    check(1646, 192, /*is_sub=*/0, 1838);  // vadd
    check(1646, 192, /*is_sub=*/1, 1454);  // vsub
    check(1970, 344, /*is_sub=*/0, 2314);  // vadd
    check(1970, 344, /*is_sub=*/1, 1626);  // vsub
    check(1107, 1830, /*is_sub=*/0, 2937);  // vadd
    check(1107, 1830, /*is_sub=*/1, 2606);  // vsub
    check(1637, 636, /*is_sub=*/0, 2273);  // vadd
    check(1637, 636, /*is_sub=*/1, 1001);  // vsub
    check(2454, 1133, /*is_sub=*/0, 258);  // vadd
    check(2454, 1133, /*is_sub=*/1, 1321);  // vsub
    check(3306, 3149, /*is_sub=*/0, 3126);  // vadd
    check(3306, 3149, /*is_sub=*/1, 157);  // vsub
    check(1290, 2341, /*is_sub=*/0, 302);  // vadd
    check(1290, 2341, /*is_sub=*/1, 2278);  // vsub
    check(122, 1806, /*is_sub=*/0, 1928);  // vadd
    check(122, 1806, /*is_sub=*/1, 1645);  // vsub
    check(372, 2250, /*is_sub=*/0, 2622);  // vadd
    check(372, 2250, /*is_sub=*/1, 1451);  // vsub
    check(2161, 1917, /*is_sub=*/0, 749);  // vadd
    check(2161, 1917, /*is_sub=*/1, 244);  // vsub
    check(1201, 1833, /*is_sub=*/0, 3034);  // vadd
    check(1201, 1833, /*is_sub=*/1, 2697);  // vsub
    check(2853, 1731, /*is_sub=*/0, 1255);  // vadd
    check(2853, 1731, /*is_sub=*/1, 1122);  // vsub
    check(1776, 2066, /*is_sub=*/0, 513);  // vadd
    check(1776, 2066, /*is_sub=*/1, 3039);  // vsub
    check(842, 2773, /*is_sub=*/0, 286);  // vadd
    check(842, 2773, /*is_sub=*/1, 1398);  // vsub
    check(698, 1681, /*is_sub=*/0, 2379);  // vadd
    check(698, 1681, /*is_sub=*/1, 2346);  // vsub
    check(2201, 2445, /*is_sub=*/0, 1317);  // vadd
    check(2201, 2445, /*is_sub=*/1, 3085);  // vsub
    check(2891, 2851, /*is_sub=*/0, 2413);  // vadd
    check(2891, 2851, /*is_sub=*/1, 40);  // vsub
    check(704, 3231, /*is_sub=*/0, 606);  // vadd
    check(704, 3231, /*is_sub=*/1, 802);  // vsub
    check(2989, 1547, /*is_sub=*/0, 1207);  // vadd
    check(2989, 1547, /*is_sub=*/1, 1442);  // vsub
    check(3135, 3087, /*is_sub=*/0, 2893);  // vadd
    check(3135, 3087, /*is_sub=*/1, 48);  // vsub
    check(580, 1036, /*is_sub=*/0, 1616);  // vadd
    check(580, 1036, /*is_sub=*/1, 2873);  // vsub
    check(2531, 2430, /*is_sub=*/0, 1632);  // vadd
    check(2531, 2430, /*is_sub=*/1, 101);  // vsub
    check(652, 1690, /*is_sub=*/0, 2342);  // vadd
    check(652, 1690, /*is_sub=*/1, 2291);  // vsub
    check(1632, 2830, /*is_sub=*/0, 1133);  // vadd
    check(1632, 2830, /*is_sub=*/1, 2131);  // vsub
    check(3104, 2644, /*is_sub=*/0, 2419);  // vadd
    check(3104, 2644, /*is_sub=*/1, 460);  // vsub
    check(91, 60, /*is_sub=*/0, 151);  // vadd
    check(91, 60, /*is_sub=*/1, 31);  // vsub
    check(1467, 2895, /*is_sub=*/0, 1033);  // vadd
    check(1467, 2895, /*is_sub=*/1, 1901);  // vsub
    check(1300, 2365, /*is_sub=*/0, 336);  // vadd
    check(1300, 2365, /*is_sub=*/1, 2264);  // vsub
    check(66, 2171, /*is_sub=*/0, 2237);  // vadd
    check(66, 2171, /*is_sub=*/1, 1224);  // vsub
    check(2389, 1741, /*is_sub=*/0, 801);  // vadd
    check(2389, 1741, /*is_sub=*/1, 648);  // vsub
    check(1159, 358, /*is_sub=*/0, 1517);  // vadd
    check(1159, 358, /*is_sub=*/1, 801);  // vsub
    check(900, 3019, /*is_sub=*/0, 590);  // vadd
    check(900, 3019, /*is_sub=*/1, 1210);  // vsub
    check(627, 585, /*is_sub=*/0, 1212);  // vadd
    check(627, 585, /*is_sub=*/1, 42);  // vsub
    check(2662, 3037, /*is_sub=*/0, 2370);  // vadd
    check(2662, 3037, /*is_sub=*/1, 2954);  // vsub
    check(1851, 2104, /*is_sub=*/0, 626);  // vadd
    check(1851, 2104, /*is_sub=*/1, 3076);  // vsub
    check(2676, 2720, /*is_sub=*/0, 2067);  // vadd
    check(2676, 2720, /*is_sub=*/1, 3285);  // vsub
    check(3047, 1783, /*is_sub=*/0, 1501);  // vadd
    check(3047, 1783, /*is_sub=*/1, 1264);  // vsub
    check(2921, 1228, /*is_sub=*/0, 820);  // vadd
    check(2921, 1228, /*is_sub=*/1, 1693);  // vsub
    check(229, 1929, /*is_sub=*/0, 2158);  // vadd
    check(229, 1929, /*is_sub=*/1, 1629);  // vsub
    check(3254, 2223, /*is_sub=*/0, 2148);  // vadd
    check(3254, 2223, /*is_sub=*/1, 1031);  // vsub
    check(1258, 2226, /*is_sub=*/0, 155);  // vadd
    check(1258, 2226, /*is_sub=*/1, 2361);  // vsub
    check(385, 2480, /*is_sub=*/0, 2865);  // vadd
    check(385, 2480, /*is_sub=*/1, 1234);  // vsub
    check(1435, 3158, /*is_sub=*/0, 1264);  // vadd
    check(1435, 3158, /*is_sub=*/1, 1606);  // vsub
    check(1588, 2426, /*is_sub=*/0, 685);  // vadd
    check(1588, 2426, /*is_sub=*/1, 2491);  // vsub
    check(381, 2804, /*is_sub=*/0, 3185);  // vadd
    check(381, 2804, /*is_sub=*/1, 906);  // vsub
    check(2508, 2921, /*is_sub=*/0, 2100);  // vadd
    check(2508, 2921, /*is_sub=*/1, 2916);  // vsub
    check(1669, 825, /*is_sub=*/0, 2494);  // vadd
    check(1669, 825, /*is_sub=*/1, 844);  // vsub
    check(1950, 2785, /*is_sub=*/0, 1406);  // vadd
    check(1950, 2785, /*is_sub=*/1, 2494);  // vsub
    check(2492, 2628, /*is_sub=*/0, 1791);  // vadd
    check(2492, 2628, /*is_sub=*/1, 3193);  // vsub
    check(2903, 1442, /*is_sub=*/0, 1016);  // vadd
    check(2903, 1442, /*is_sub=*/1, 1461);  // vsub
    check(1989, 3216, /*is_sub=*/0, 1876);  // vadd
    check(1989, 3216, /*is_sub=*/1, 2102);  // vsub
    check(90, 651, /*is_sub=*/0, 741);  // vadd
    check(90, 651, /*is_sub=*/1, 2768);  // vsub
    check(909, 763, /*is_sub=*/0, 1672);  // vadd
    check(909, 763, /*is_sub=*/1, 146);  // vsub
    check(335, 2061, /*is_sub=*/0, 2396);  // vadd
    check(335, 2061, /*is_sub=*/1, 1603);  // vsub
    check(2681, 1578, /*is_sub=*/0, 930);  // vadd
    check(2681, 1578, /*is_sub=*/1, 1103);  // vsub
    check(1236, 2442, /*is_sub=*/0, 349);  // vadd
    check(1236, 2442, /*is_sub=*/1, 2123);  // vsub
    check(1564, 1246, /*is_sub=*/0, 2810);  // vadd
    check(1564, 1246, /*is_sub=*/1, 318);  // vsub
    check(2827, 1140, /*is_sub=*/0, 638);  // vadd
    check(2827, 1140, /*is_sub=*/1, 1687);  // vsub
    check(2363, 1688, /*is_sub=*/0, 722);  // vadd
    check(2363, 1688, /*is_sub=*/1, 675);  // vsub
    check(2702, 644, /*is_sub=*/0, 17);  // vadd
    check(2702, 644, /*is_sub=*/1, 2058);  // vsub
    check(1476, 3055, /*is_sub=*/0, 1202);  // vadd
    check(1476, 3055, /*is_sub=*/1, 1750);  // vsub
    check(2690, 2368, /*is_sub=*/0, 1729);  // vadd
    check(2690, 2368, /*is_sub=*/1, 322);  // vsub
    check(919, 1562, /*is_sub=*/0, 2481);  // vadd
    check(919, 1562, /*is_sub=*/1, 2686);  // vsub
    check(2485, 2188, /*is_sub=*/0, 1344);  // vadd
    check(2485, 2188, /*is_sub=*/1, 297);  // vsub
    check(1181, 1161, /*is_sub=*/0, 2342);  // vadd
    check(1181, 1161, /*is_sub=*/1, 20);  // vsub
    check(2919, 1253, /*is_sub=*/0, 843);  // vadd
    check(2919, 1253, /*is_sub=*/1, 1666);  // vsub
    check(2214, 715, /*is_sub=*/0, 2929);  // vadd
    check(2214, 715, /*is_sub=*/1, 1499);  // vsub
    check(1610, 815, /*is_sub=*/0, 2425);  // vadd
    check(1610, 815, /*is_sub=*/1, 795);  // vsub
    check(1767, 2472, /*is_sub=*/0, 910);  // vadd
    check(1767, 2472, /*is_sub=*/1, 2624);  // vsub
    check(1450, 1306, /*is_sub=*/0, 2756);  // vadd
    check(1450, 1306, /*is_sub=*/1, 144);  // vsub
    check(2839, 2210, /*is_sub=*/0, 1720);  // vadd
    check(2839, 2210, /*is_sub=*/1, 629);  // vsub
    check(627, 1936, /*is_sub=*/0, 2563);  // vadd
    check(627, 1936, /*is_sub=*/1, 2020);  // vsub
    check(2808, 1622, /*is_sub=*/0, 1101);  // vadd
    check(2808, 1622, /*is_sub=*/1, 1186);  // vsub
    check(1124, 2403, /*is_sub=*/0, 198);  // vadd
    check(1124, 2403, /*is_sub=*/1, 2050);  // vsub
    check(1300, 2103, /*is_sub=*/0, 74);  // vadd
    check(1300, 2103, /*is_sub=*/1, 2526);  // vsub
    check(2606, 3097, /*is_sub=*/0, 2374);  // vadd
    check(2606, 3097, /*is_sub=*/1, 2838);  // vsub
    check(8, 1403, /*is_sub=*/0, 1411);  // vadd
    check(8, 1403, /*is_sub=*/1, 1934);  // vsub
    check(2715, 1835, /*is_sub=*/0, 1221);  // vadd
    check(2715, 1835, /*is_sub=*/1, 880);  // vsub
    check(2505, 2426, /*is_sub=*/0, 1602);  // vadd
    check(2505, 2426, /*is_sub=*/1, 79);  // vsub
    check(1053, 159, /*is_sub=*/0, 1212);  // vadd
    check(1053, 159, /*is_sub=*/1, 894);  // vsub
    check(565, 1926, /*is_sub=*/0, 2491);  // vadd
    check(565, 1926, /*is_sub=*/1, 1968);  // vsub
    check(1290, 3308, /*is_sub=*/0, 1269);  // vadd
    check(1290, 3308, /*is_sub=*/1, 1311);  // vsub
    check(2031, 1111, /*is_sub=*/0, 3142);  // vadd
    check(2031, 1111, /*is_sub=*/1, 920);  // vsub
    check(32, 1526, /*is_sub=*/0, 1558);  // vadd
    check(32, 1526, /*is_sub=*/1, 1835);  // vsub
    check(3170, 1363, /*is_sub=*/0, 1204);  // vadd
    check(3170, 1363, /*is_sub=*/1, 1807);  // vsub
    check(2438, 729, /*is_sub=*/0, 3167);  // vadd
    check(2438, 729, /*is_sub=*/1, 1709);  // vsub
    check(1217, 2500, /*is_sub=*/0, 388);  // vadd
    check(1217, 2500, /*is_sub=*/1, 2046);  // vsub
    check(806, 3037, /*is_sub=*/0, 514);  // vadd
    check(806, 3037, /*is_sub=*/1, 1098);  // vsub
    check(627, 610, /*is_sub=*/0, 1237);  // vadd
    check(627, 610, /*is_sub=*/1, 17);  // vsub
    check(1214, 3077, /*is_sub=*/0, 962);  // vadd
    check(1214, 3077, /*is_sub=*/1, 1466);  // vsub
    check(2779, 2129, /*is_sub=*/0, 1579);  // vadd
    check(2779, 2129, /*is_sub=*/1, 650);  // vsub
    check(2617, 1325, /*is_sub=*/0, 613);  // vadd
    check(2617, 1325, /*is_sub=*/1, 1292);  // vsub
    check(1943, 1151, /*is_sub=*/0, 3094);  // vadd
    check(1943, 1151, /*is_sub=*/1, 792);  // vsub
    check(2373, 3249, /*is_sub=*/0, 2293);  // vadd
    check(2373, 3249, /*is_sub=*/1, 2453);  // vsub
    check(2493, 1632, /*is_sub=*/0, 796);  // vadd
    check(2493, 1632, /*is_sub=*/1, 861);  // vsub
    check(3082, 13, /*is_sub=*/0, 3095);  // vadd
    check(3082, 13, /*is_sub=*/1, 3069);  // vsub
    check(1942, 2379, /*is_sub=*/0, 992);  // vadd
    check(1942, 2379, /*is_sub=*/1, 2892);  // vsub
    check(3228, 247, /*is_sub=*/0, 146);  // vadd
    check(3228, 247, /*is_sub=*/1, 2981);  // vsub
    check(3091, 2954, /*is_sub=*/0, 2716);  // vadd
    check(3091, 2954, /*is_sub=*/1, 137);  // vsub
    check(1730, 2918, /*is_sub=*/0, 1319);  // vadd
    check(1730, 2918, /*is_sub=*/1, 2141);  // vsub
    check(3119, 2288, /*is_sub=*/0, 2078);  // vadd
    check(3119, 2288, /*is_sub=*/1, 831);  // vsub
    check(917, 1807, /*is_sub=*/0, 2724);  // vadd
    check(917, 1807, /*is_sub=*/1, 2439);  // vsub
    check(246, 843, /*is_sub=*/0, 1089);  // vadd
    check(246, 843, /*is_sub=*/1, 2732);  // vsub
    check(1629, 2538, /*is_sub=*/0, 838);  // vadd
    check(1629, 2538, /*is_sub=*/1, 2420);  // vsub
    check(3323, 320, /*is_sub=*/0, 314);  // vadd
    check(3323, 320, /*is_sub=*/1, 3003);  // vsub
    check(612, 1875, /*is_sub=*/0, 2487);  // vadd
    check(612, 1875, /*is_sub=*/1, 2066);  // vsub
    check(73, 2473, /*is_sub=*/0, 2546);  // vadd
    check(73, 2473, /*is_sub=*/1, 929);  // vsub
    check(657, 954, /*is_sub=*/0, 1611);  // vadd
    check(657, 954, /*is_sub=*/1, 3032);  // vsub
    check(361, 106, /*is_sub=*/0, 467);  // vadd
    check(361, 106, /*is_sub=*/1, 255);  // vsub
    check(120, 64, /*is_sub=*/0, 184);  // vadd
    check(120, 64, /*is_sub=*/1, 56);  // vsub
    check(224, 429, /*is_sub=*/0, 653);  // vadd
    check(224, 429, /*is_sub=*/1, 3124);  // vsub
    check(1398, 2108, /*is_sub=*/0, 177);  // vadd
    check(1398, 2108, /*is_sub=*/1, 2619);  // vsub
    check(23, 2041, /*is_sub=*/0, 2064);  // vadd
    check(23, 2041, /*is_sub=*/1, 1311);  // vsub
    check(824, 1364, /*is_sub=*/0, 2188);  // vadd
    check(824, 1364, /*is_sub=*/1, 2789);  // vsub
    check(3244, 1842, /*is_sub=*/0, 1757);  // vadd
    check(3244, 1842, /*is_sub=*/1, 1402);  // vsub
    check(2624, 3194, /*is_sub=*/0, 2489);  // vadd
    check(2624, 3194, /*is_sub=*/1, 2759);  // vsub
    check(2441, 2761, /*is_sub=*/0, 1873);  // vadd
    check(2441, 2761, /*is_sub=*/1, 3009);  // vsub
    check(2025, 1387, /*is_sub=*/0, 83);  // vadd
    check(2025, 1387, /*is_sub=*/1, 638);  // vsub
    check(2428, 1917, /*is_sub=*/0, 1016);  // vadd
    check(2428, 1917, /*is_sub=*/1, 511);  // vsub
    check(2080, 96, /*is_sub=*/0, 2176);  // vadd
    check(2080, 96, /*is_sub=*/1, 1984);  // vsub
    check(2671, 1805, /*is_sub=*/0, 1147);  // vadd
    check(2671, 1805, /*is_sub=*/1, 866);  // vsub
    check(2039, 546, /*is_sub=*/0, 2585);  // vadd
    check(2039, 546, /*is_sub=*/1, 1493);  // vsub
    check(2289, 1180, /*is_sub=*/0, 140);  // vadd
    check(2289, 1180, /*is_sub=*/1, 1109);  // vsub
    check(148, 2725, /*is_sub=*/0, 2873);  // vadd
    check(148, 2725, /*is_sub=*/1, 752);  // vsub
    check(1381, 832, /*is_sub=*/0, 2213);  // vadd
    check(1381, 832, /*is_sub=*/1, 549);  // vsub
    check(2946, 1975, /*is_sub=*/0, 1592);  // vadd
    check(2946, 1975, /*is_sub=*/1, 971);  // vsub
    check(348, 2982, /*is_sub=*/0, 1);  // vadd
    check(348, 2982, /*is_sub=*/1, 695);  // vsub
    check(888, 1645, /*is_sub=*/0, 2533);  // vadd
    check(888, 1645, /*is_sub=*/1, 2572);  // vsub
    check(3268, 871, /*is_sub=*/0, 810);  // vadd
    check(3268, 871, /*is_sub=*/1, 2397);  // vsub
    check(182, 3229, /*is_sub=*/0, 82);  // vadd
    check(182, 3229, /*is_sub=*/1, 282);  // vsub
    check(2495, 1326, /*is_sub=*/0, 492);  // vadd
    check(2495, 1326, /*is_sub=*/1, 1169);  // vsub
    check(325, 1476, /*is_sub=*/0, 1801);  // vadd
    check(325, 1476, /*is_sub=*/1, 2178);  // vsub
    check(2723, 839, /*is_sub=*/0, 233);  // vadd
    check(2723, 839, /*is_sub=*/1, 1884);  // vsub
    check(2542, 2049, /*is_sub=*/0, 1262);  // vadd
    check(2542, 2049, /*is_sub=*/1, 493);  // vsub
    check(725, 1019, /*is_sub=*/0, 1744);  // vadd
    check(725, 1019, /*is_sub=*/1, 3035);  // vsub
    check(2833, 2278, /*is_sub=*/0, 1782);  // vadd
    check(2833, 2278, /*is_sub=*/1, 555);  // vsub
    check(2673, 516, /*is_sub=*/0, 3189);  // vadd
    check(2673, 516, /*is_sub=*/1, 2157);  // vsub
    check(820, 3267, /*is_sub=*/0, 758);  // vadd
    check(820, 3267, /*is_sub=*/1, 882);  // vsub
    check(1970, 759, /*is_sub=*/0, 2729);  // vadd
    check(1970, 759, /*is_sub=*/1, 1211);  // vsub
    check(2248, 1282, /*is_sub=*/0, 201);  // vadd
    check(2248, 1282, /*is_sub=*/1, 966);  // vsub
    check(705, 414, /*is_sub=*/0, 1119);  // vadd
    check(705, 414, /*is_sub=*/1, 291);  // vsub
    check(1037, 379, /*is_sub=*/0, 1416);  // vadd
    check(1037, 379, /*is_sub=*/1, 658);  // vsub
    check(2530, 3251, /*is_sub=*/0, 2452);  // vadd
    check(2530, 3251, /*is_sub=*/1, 2608);  // vsub
    check(3161, 2366, /*is_sub=*/0, 2198);  // vadd
    check(3161, 2366, /*is_sub=*/1, 795);  // vsub
    check(1050, 2971, /*is_sub=*/0, 692);  // vadd
    check(1050, 2971, /*is_sub=*/1, 1408);  // vsub
    check(2998, 469, /*is_sub=*/0, 138);  // vadd
    check(2998, 469, /*is_sub=*/1, 2529);  // vsub
    check(2621, 148, /*is_sub=*/0, 2769);  // vadd
    check(2621, 148, /*is_sub=*/1, 2473);  // vsub
    check(2753, 3182, /*is_sub=*/0, 2606);  // vadd
    check(2753, 3182, /*is_sub=*/1, 2900);  // vsub
    check(271, 56, /*is_sub=*/0, 327);  // vadd
    check(271, 56, /*is_sub=*/1, 215);  // vsub
    check(447, 3026, /*is_sub=*/0, 144);  // vadd
    check(447, 3026, /*is_sub=*/1, 750);  // vsub
    check(1743, 2732, /*is_sub=*/0, 1146);  // vadd
    check(1743, 2732, /*is_sub=*/1, 2340);  // vsub
    check(2476, 2741, /*is_sub=*/0, 1888);  // vadd
    check(2476, 2741, /*is_sub=*/1, 3064);  // vsub
    check(2168, 2594, /*is_sub=*/0, 1433);  // vadd
    check(2168, 2594, /*is_sub=*/1, 2903);  // vsub
    check(508, 917, /*is_sub=*/0, 1425);  // vadd
    check(508, 917, /*is_sub=*/1, 2920);  // vsub
    check(871, 1384, /*is_sub=*/0, 2255);  // vadd
    check(871, 1384, /*is_sub=*/1, 2816);  // vsub
    check(2878, 215, /*is_sub=*/0, 3093);  // vadd
    check(2878, 215, /*is_sub=*/1, 2663);  // vsub
    check(888, 41, /*is_sub=*/0, 929);  // vadd
    check(888, 41, /*is_sub=*/1, 847);  // vsub
    check(1146, 1405, /*is_sub=*/0, 2551);  // vadd
    check(1146, 1405, /*is_sub=*/1, 3070);  // vsub
    check(2922, 3028, /*is_sub=*/0, 2621);  // vadd
    check(2922, 3028, /*is_sub=*/1, 3223);  // vsub
    check(2909, 2555, /*is_sub=*/0, 2135);  // vadd
    check(2909, 2555, /*is_sub=*/1, 354);  // vsub
    check(2969, 685, /*is_sub=*/0, 325);  // vadd
    check(2969, 685, /*is_sub=*/1, 2284);  // vsub
    check(1254, 564, /*is_sub=*/0, 1818);  // vadd
    check(1254, 564, /*is_sub=*/1, 690);  // vsub
    check(3290, 2500, /*is_sub=*/0, 2461);  // vadd
    check(3290, 2500, /*is_sub=*/1, 790);  // vsub
    check(3113, 1344, /*is_sub=*/0, 1128);  // vadd
    check(3113, 1344, /*is_sub=*/1, 1769);  // vsub
    check(1336, 2312, /*is_sub=*/0, 319);  // vadd
    check(1336, 2312, /*is_sub=*/1, 2353);  // vsub
    check(1366, 2383, /*is_sub=*/0, 420);  // vadd
    check(1366, 2383, /*is_sub=*/1, 2312);  // vsub
    check(1284, 580, /*is_sub=*/0, 1864);  // vadd
    check(1284, 580, /*is_sub=*/1, 704);  // vsub
    check(631, 2765, /*is_sub=*/0, 67);  // vadd
    check(631, 2765, /*is_sub=*/1, 1195);  // vsub
    check(1798, 2054, /*is_sub=*/0, 523);  // vadd
    check(1798, 2054, /*is_sub=*/1, 3073);  // vsub
    check(2552, 193, /*is_sub=*/0, 2745);  // vadd
    check(2552, 193, /*is_sub=*/1, 2359);  // vsub
    check(2243, 2862, /*is_sub=*/0, 1776);  // vadd
    check(2243, 2862, /*is_sub=*/1, 2710);  // vsub
    check(669, 605, /*is_sub=*/0, 1274);  // vadd
    check(669, 605, /*is_sub=*/1, 64);  // vsub
    check(2072, 2148, /*is_sub=*/0, 891);  // vadd
    check(2072, 2148, /*is_sub=*/1, 3253);  // vsub
    check(1223, 1039, /*is_sub=*/0, 2262);  // vadd
    check(1223, 1039, /*is_sub=*/1, 184);  // vsub
    check(2228, 1618, /*is_sub=*/0, 517);  // vadd
    check(2228, 1618, /*is_sub=*/1, 610);  // vsub
    check(2961, 655, /*is_sub=*/0, 287);  // vadd
    check(2961, 655, /*is_sub=*/1, 2306);  // vsub
    check(1900, 3128, /*is_sub=*/0, 1699);  // vadd
    check(1900, 3128, /*is_sub=*/1, 2101);  // vsub
    check(3083, 1997, /*is_sub=*/0, 1751);  // vadd
    check(3083, 1997, /*is_sub=*/1, 1086);  // vsub
    check(723, 2706, /*is_sub=*/0, 100);  // vadd
    check(723, 2706, /*is_sub=*/1, 1346);  // vsub
    check(2330, 1890, /*is_sub=*/0, 891);  // vadd
    check(2330, 1890, /*is_sub=*/1, 440);  // vsub
    check(664, 3229, /*is_sub=*/0, 564);  // vadd
    check(664, 3229, /*is_sub=*/1, 764);  // vsub
    check(3205, 14, /*is_sub=*/0, 3219);  // vadd
    check(3205, 14, /*is_sub=*/1, 3191);  // vsub
    check(3264, 2271, /*is_sub=*/0, 2206);  // vadd
    check(3264, 2271, /*is_sub=*/1, 993);  // vsub
    check(2, 717, /*is_sub=*/0, 719);  // vadd
    check(2, 717, /*is_sub=*/1, 2614);  // vsub
    check(1316, 3267, /*is_sub=*/0, 1254);  // vadd
    check(1316, 3267, /*is_sub=*/1, 1378);  // vsub
    check(1490, 2717, /*is_sub=*/0, 878);  // vadd
    check(1490, 2717, /*is_sub=*/1, 2102);  // vsub
    check(1686, 2603, /*is_sub=*/0, 960);  // vadd
    check(1686, 2603, /*is_sub=*/1, 2412);  // vsub
    check(1263, 954, /*is_sub=*/0, 2217);  // vadd
    check(1263, 954, /*is_sub=*/1, 309);  // vsub
    check(2758, 1983, /*is_sub=*/0, 1412);  // vadd
    check(2758, 1983, /*is_sub=*/1, 775);  // vsub
    check(1899, 2241, /*is_sub=*/0, 811);  // vadd
    check(1899, 2241, /*is_sub=*/1, 2987);  // vsub
    check(3195, 3191, /*is_sub=*/0, 3057);  // vadd
    check(3195, 3191, /*is_sub=*/1, 4);  // vsub
    check(297, 3300, /*is_sub=*/0, 268);  // vadd
    check(297, 3300, /*is_sub=*/1, 326);  // vsub
    check(2698, 270, /*is_sub=*/0, 2968);  // vadd
    check(2698, 270, /*is_sub=*/1, 2428);  // vsub
    check(2839, 342, /*is_sub=*/0, 3181);  // vadd
    check(2839, 342, /*is_sub=*/1, 2497);  // vsub
    check(1599, 1957, /*is_sub=*/0, 227);  // vadd
    check(1599, 1957, /*is_sub=*/1, 2971);  // vsub
    check(442, 1, /*is_sub=*/0, 443);  // vadd
    check(442, 1, /*is_sub=*/1, 441);  // vsub
    check(2211, 183, /*is_sub=*/0, 2394);  // vadd
    check(2211, 183, /*is_sub=*/1, 2028);  // vsub
    check(2539, 2943, /*is_sub=*/0, 2153);  // vadd
    check(2539, 2943, /*is_sub=*/1, 2925);  // vsub
    check(3196, 1538, /*is_sub=*/0, 1405);  // vadd
    check(3196, 1538, /*is_sub=*/1, 1658);  // vsub
    check(548, 901, /*is_sub=*/0, 1449);  // vadd
    check(548, 901, /*is_sub=*/1, 2976);  // vsub
    check(556, 320, /*is_sub=*/0, 876);  // vadd
    check(556, 320, /*is_sub=*/1, 236);  // vsub
    check(1311, 2441, /*is_sub=*/0, 423);  // vadd
    check(1311, 2441, /*is_sub=*/1, 2199);  // vsub
    check(3253, 1946, /*is_sub=*/0, 1870);  // vadd
    check(3253, 1946, /*is_sub=*/1, 1307);  // vsub
    check(1397, 1219, /*is_sub=*/0, 2616);  // vadd
    check(1397, 1219, /*is_sub=*/1, 178);  // vsub
